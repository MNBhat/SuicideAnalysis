# install with conda env create -f analysis.yaml
name: 'analysis-532'
channels:
  - conda-forge
  - defaults
dependencies:
  - ipykernel
  - ipython>=7.15
  - pandas
  - pip
  - numpy
  - dash=2.8.*
  - dash-bootstrap-components
  - altair
  - werkzeug==2.0.3
    